# Binary to Decimal Converter

This is a simple Python program that converts a binary number into its decimal equivalent.

## How it works
The program reads a binary number as input and calculates the decimal value using positional weights of base 2.

## How to run

```bash
python binary_to_decimal.py
